// src/types/index.ts
//
// Ponto de entrada dos tipos do projeto. O modelo de dados do CMS
// (páginas, setores, obras, posts) vive em ./content.ts — reexportado
// aqui para que o resto do app possa importar de 'src/types' direto.

export * from './content';
